<% provide(:title, "トップページ") %>
<div class="center jumbotron">
  <h1>Financial Planner Booking</h1>
  <% if not fp_signed_in? %>
    <%= link_to "アカウント登録", new_user_registration_path, class: "btn btn-lg btn-success" %>
  <% end %>
</div>
